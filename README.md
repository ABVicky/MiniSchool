# MINI SCHOOL — Frontend (React + Tailwind)

This folder contains a ready-to-run frontend project for the MINI SCHOOL site.
It includes generated sample images (programmatic placeholders) in `src/assets/gallery/`.

## Quick start
1. Install dependencies: `npm i`
2. Run dev server: `npm run dev`
3. Build: `npm run build`

> Note: Tailwind/PostCSS and Vite are configured in this repo.

